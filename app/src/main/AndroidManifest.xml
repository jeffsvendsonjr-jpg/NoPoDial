<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Wake on call state changes (no polling, no foreground service). -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- Find the number that was just dialed, after the call ends. -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <!-- Fire the apology text. -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <!-- Tell the user an apology was sent (Android 13+). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Telephony is what the app is for, but it isn't required to install,
         so the UI can run on Wi-Fi-only tablets for development and demo. -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoPoDial">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Only fires on actual call state transitions; idle cost is zero. -->
        <receiver
            android:name=".CallStateReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_PHONE_STATE">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
