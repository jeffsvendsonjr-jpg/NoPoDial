<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Wake on call state changes (no polling, no foreground service). -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- Recents list, and finding the number that was just dialed/answered. -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <!-- Fire the pocket-dial apology text. -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    <!-- Call-note prompts, follow-up reminders, apology confirmations (Android 13+). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Contacts tab is a read-only view over the system contacts. -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- Place calls straight from the dialpad (falls back to ACTION_DIAL). -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <!-- Callback reminders fire on time, not whenever Doze feels like it. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Reminders are rescheduled after a reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Telephony is what the app is for, but it isn't required to install,
         so the UI can run on Wi-Fi-only tablets for development and demo. -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoPoDial">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <activity
            android:name=".journal.NoteEditorActivity"
            android:exported="false" />

        <activity
            android:name=".ui.ContactDetailActivity"
            android:exported="false" />

        <!-- Only fires on actual call state transitions; idle cost is zero. -->
        <receiver
            android:name=".CallStateReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_PHONE_STATE">
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>

        <!-- Follow-up reminder alarms and their Done/Snooze actions. -->
        <receiver
            android:name=".journal.ReminderReceiver"
            android:exported="false" />

        <!-- Alarms don't survive reboot; this brings pending reminders back. -->
        <receiver
            android:name=".journal.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
